---
name: accessibility-gate
description: Use when working on forms, buttons, links, navigation, menus, modals, tabs, accordions, icon-only controls, keyboard/focus behavior, labels, semantics, or accessibility concerns. Runs axe when a URL is available and requires manual review where automation is insufficient.
---

# Accessibility Gate

Use `accessibility-check` if a URL is available. Use axe as an automated check, but do not claim full accessibility from axe alone. Check keyboard operation, focus visibility/order, labels, roles, names, states, and semantics manually when relevant. Report limitations.
