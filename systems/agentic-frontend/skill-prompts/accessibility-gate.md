# Accessibility Gate

Use when working on forms, buttons, links, nav, menus, modals, tabs, accordions, icon-only controls, or accessibility concerns.

Rules:
- Use `accessibility-check` script if URL is available.
- Use axe as the automated check.
- Do not claim full accessibility from axe alone.
- Check keyboard/focus/labels/semantics manually when relevant.
- Log limitations.
