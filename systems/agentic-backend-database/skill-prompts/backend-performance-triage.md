# Backend Performance Triage

Use for slow APIs, DB queries, queues, and heavy payloads.

Rules: identify bottleneck before optimizing; check query patterns, indexes, payload size, caching, and external calls; do not add caching blindly; do not change schema blindly; suggest measurable verification.
